apiVersion: v1
kind: Service
metadata:
  name: incident-copilot
spec:
  selector:
    app: incident-copilot
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
  type: {{ .Values.service.type }}